{{- if and (eq .Values.redis.architecture "replication") (not .Values.redis.sentinel.enabled) }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-redis
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}-redis
spec:
  serviceName: {{ .Release.Name }}-redis-headless
  replicas: {{ .Values.redis.master.count | default 3 }}
  podManagementPolicy: Parallel
  selector:
    matchLabels:
      app: {{ .Release.Name }}-redis
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-redis
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/redis/redis-config.yaml") . | sha256sum }}
    spec:
      {{- with .Values.redis.podSecurityContext }}
      securityContext:
        runAsUser: {{ .runAsUser }}
        fsGroup: {{ .fsGroup }}
      {{- end }}
      initContainers:
      - name: fix-permissions
        image: {{ .Values.redis.image.repository }}:{{ .Values.redis.image.tag }}
        imagePullPolicy: {{ .Values.redis.image.pullPolicy }}
        command:
        - sh
        - -c
        - |
          echo "=== Fixing permissions ==="
          chown -R {{ .Values.redis.podSecurityContext.runAsUser }}:{{ .Values.redis.podSecurityContext.fsGroup }} /data
          chmod -R 755 /data
          echo "=== Done ==="
        volumeMounts:
        - name: redis-data
          mountPath: /data
        securityContext:
          runAsUser: 0
      containers:
      - name: redis
        image: {{ .Values.redis.image.repository }}:{{ .Values.redis.image.tag }}
        imagePullPolicy: {{ .Values.redis.image.pullPolicy }}
        ports:
        - containerPort: 6379
          name: redis
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        {{- if .Values.redis.auth.enabled }}
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              {{- if .Values.redis.auth.existingSecret }}
              name: {{ .Values.redis.auth.existingSecret }}
              key: {{ .Values.redis.auth.existingSecretPasswordKey | default "password" }}
              {{- else }}
              name: {{ .Release.Name }}-redis-secret
              key: password
              {{- end }}
        {{- end }}
        command:
        - sh
        - -c
        args:
        - |
          # Copy base config
          cp /etc/redis/redis.conf /tmp/redis.conf

          {{- if .Values.redis.auth.enabled }}
          # Add auth
          echo "requirepass $REDIS_PASSWORD" >> /tmp/redis.conf
          echo "masterauth $REDIS_PASSWORD" >> /tmp/redis.conf
          {{- end }}

          # Determine if this is master or replica
          HOSTNAME=$(hostname)
          if [[ "$HOSTNAME" == *-0 ]]; then
            echo "=== Starting as MASTER ==="
          else
            echo "=== Starting as REPLICA ==="
            echo "replicaof {{ .Release.Name }}-redis-0.{{ .Release.Name }}-redis-headless 6379" >> /tmp/redis.conf
          fi

          # Announce pod IP for proper replication
          echo "replica-announce-ip $POD_IP" >> /tmp/redis.conf

          # Start Redis
          exec redis-server /tmp/redis.conf
        volumeMounts:
        - name: redis-data
          mountPath: /data
        - name: redis-config
          mountPath: /etc/redis
        resources:
          {{- toYaml .Values.redis.resources | nindent 10 }}
        livenessProbe:
          exec:
            command:
            - sh
            - -c
            - |
              {{- if .Values.redis.auth.enabled }}
              redis-cli -a "$REDIS_PASSWORD" ping
              {{- else }}
              redis-cli ping
              {{- end }}
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - sh
            - -c
            - |
              {{- if .Values.redis.auth.enabled }}
              redis-cli -a "$REDIS_PASSWORD" ping
              {{- else }}
              redis-cli ping
              {{- end }}
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: redis-config
        configMap:
          name: {{ .Release.Name }}-redis-config
  {{- if .Values.redis.storage.enabled }}
  volumeClaimTemplates:
  - metadata:
      name: redis-data
    spec:
      accessModes: {{ toYaml .Values.redis.storage.accessModes | nindent 8 }}
      {{- if .Values.redis.storage.storageClassName }}
      storageClassName: {{ .Values.redis.storage.storageClassName }}
      {{- end }}
      resources:
        requests:
          storage: {{ .Values.redis.storage.size }}
  {{- else }}
  # Fallback to emptyDir if storage disabled
  # Note: This means data loss on pod restart
  {{- end }}
{{- end }}