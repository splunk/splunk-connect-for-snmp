{{- if .Values.otel.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "splunk-connect-for-snmp.otel.fullname" . }}
  labels:
    {{- include "splunk-connect-for-snmp.otel.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "splunk-connect-for-snmp.otel.fullname" . }}
  minReplicas: {{ .Values.otel.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.otel.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.otel.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.otel.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.otel.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.otel.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
