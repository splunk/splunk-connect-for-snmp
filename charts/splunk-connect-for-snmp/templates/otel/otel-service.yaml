{{- if .Values.otel.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: otel-service
  namespace: {{ .Release.Namespace | quote }}
spec:
  type: ClusterIP
  ports:
    - port: 8882
      name: metrics
    - port: 8883
      name: logs
  selector:
    app: sc4snmp-otel
{{- end }}