{{- if and .Values.splunk.token (not .Values.splunk.tokenSecretRef.name) (not .Values.splunk.tokenFilePath) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "splunk-connect-for-snmp.name" . }}-splunk
type: Opaque
data:
  hec_token: {{ .Values.splunk.token | b64enc | quote }}
{{- end }}
