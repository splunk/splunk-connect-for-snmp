{{- if eq .Values.mongodb.mode "standalone" }}

{{- $bitnamiPVC := printf "datadir-%s-mongodb-0" .Release.Name }}
{{- $existingBitnamiPVC := lookup "v1" "PersistentVolumeClaim" .Release.Namespace $bitnamiPVC }}
{{- $pvcName := ternary "datadir" "data" (not (empty $existingBitnamiPVC)) }}

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-mongodb
  labels:
    {{- include "splunk-connect-for-snmp.labels" . | nindent 4 }}
    app.kubernetes.io/name: {{ .Release.Name }}-mongodb
spec:
  serviceName: {{ .Release.Name }}-mongodb
  replicas: 1

  selector:
    matchLabels:
      {{- include "splunk-connect-for-snmp.mongodb.selectorLabels" . | nindent 6 }}

  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-mongodb
        mode: standalone
    spec:
      {{- with .Values.mongodb.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      initContainers:
      {{- if .Values.mongodb.initPermissions }}
      - name: init-permissions
        image: busybox:1.36
        command: ["sh", "-c", "mkdir -p /data/db && chown -R 999:999 /data/db && chmod -R 755 /data/db"]
        volumeMounts:
        - name: {{ $pvcName }}
          mountPath: /data/db
        securityContext:
          runAsUser: 0
      {{- end }}

      containers:
      - name: mongodb
        image: "{{ .Values.mongodb.image.repository }}:{{ .Values.mongodb.image.tag }}"
        imagePullPolicy: {{ .Values.mongodb.image.pullPolicy | default "IfNotPresent" }}

        {{- with .Values.mongodb.containerSecurityContext }}
        securityContext:
          {{- toYaml . | nindent 10 }}
        {{- end }}

        ports:
        - containerPort: 27017
          name: mongodb

        {{- if .Values.mongodb.auth.enabled }}
        env:
         {{- if .Values.mongodb.auth.existingSecret }}
          - name: MONGO_INITDB_ROOT_USERNAME
            valueFrom:
              secretKeyRef:
                name: {{ .Values.mongodb.auth.existingSecret }}
                key: root-user
          - name: MONGO_INITDB_ROOT_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Values.mongodb.auth.existingSecret }}
                key: root-password
          {{- else }}
          - name: MONGO_INITDB_ROOT_USERNAME
            valueFrom:
              secretKeyRef:
                name: {{ .Release.Name }}-mongodb-secret
                key: root-user
          - name: MONGO_INITDB_ROOT_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Release.Name }}-mongodb-secret
                key: root-password
        {{- end }}
        {{- end }}

        volumeMounts:
        - name: {{ $pvcName }}
          mountPath: /data/db

        {{- with .Values.mongodb.resources }}
        resources:
          {{- toYaml . | nindent 10 }}
        {{- end }}

        {{- if .Values.mongodb.livenessProbe.enabled }}
        livenessProbe:
          exec:
            command:
            - mongosh
            - --eval
            - "db.adminCommand('ping')"
          initialDelaySeconds: {{ .Values.mongodb.livenessProbe.initialDelaySeconds | default 30 }}
          periodSeconds: {{ .Values.mongodb.livenessProbe.periodSeconds | default 10 }}
          timeoutSeconds: {{ .Values.mongodb.livenessProbe.timeoutSeconds | default 5 }}
          failureThreshold: {{ .Values.mongodb.livenessProbe.failureThreshold | default 3 }}
        {{- end }}

        {{- if .Values.mongodb.readinessProbe.enabled }}
        readinessProbe:
          exec:
            command:
            - mongosh
            - --eval
            - "db.adminCommand('ping')"
          initialDelaySeconds: {{ .Values.mongodb.readinessProbe.initialDelaySeconds | default 5 }}
          periodSeconds: {{ .Values.mongodb.readinessProbe.periodSeconds | default 10 }}
          timeoutSeconds: {{ .Values.mongodb.readinessProbe.timeoutSeconds | default 5 }}
          failureThreshold: {{ .Values.mongodb.readinessProbe.failureThreshold | default 3 }}
        {{- end }}

  {{- if .Values.mongodb.persistence.enabled }}
  volumeClaimTemplates:
  - metadata:
      name: {{ $pvcName }}
      {{- if $existingBitnamiPVC }}
      annotations:
        migration.mongodb/bitnami-pvc: {{ $bitnamiPVC | quote }}
      {{- end }}
    spec:
      accessModes:
      - {{ .Values.mongodb.persistence.accessMode | default "ReadWriteOnce" }}
      {{- if .Values.mongodb.persistence.storageClassName }}
      storageClassName: {{ .Values.mongodb.persistence.storageClassName }}
      {{- end }}
      resources:
        requests:
          storage: {{ .Values.mongodb.persistence.size | default "10Gi" }}
  {{- end }}
{{- end }}