apiVersion: v1
kind: Service
metadata:
  name: sc4-snmp-traps
spec:
  externalTrafficPolicy: Local
  type: LoadBalancer
  # loadBalancerIP: replace-me
  ports:
    - port: 162
      targetPort: 2162
      protocol: UDP
  selector:
    app: sc4-snmp-traps
---
