apiVersion: v1
kind: ConfigMap
metadata:
  name: sc4snmp-trap
  namespace: {{ .Release.Namespace | quote }}
data:
  config.yaml: |-
    # Splunk Connect for SNMP Traps
    # Sample Configuration file
    snmp:
      thread-pool:
        max-suggested-working-threads: 10
{{ .Values.files.traps.config | indent 6 }}
